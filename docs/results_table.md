# Comparative Results

Placeholder — generated by `python -m src.generate_results_table` after
`python -m src.train --model all` has produced `results/<model>/all_fold_metrics.json`
for every model in `config.yaml::models_compared`.
